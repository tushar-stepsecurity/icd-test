name: s08 Unprovable absence, 521 branches

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit
      - name: Action under test
        uses: tushar-stepsecurity/icd-test-bigrepo@44f5d4435dae0a65d464388b5ed076af8539a571
